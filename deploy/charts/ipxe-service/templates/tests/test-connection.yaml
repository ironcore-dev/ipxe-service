apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "ipxe-service.fullname" . }}-test-connection"
  labels:
    {{- include "ipxe-service.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "ipxe-service.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
